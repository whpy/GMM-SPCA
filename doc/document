此方法是根据论文Learning_dominant_physical_processes_with_data_driven_balance_models的描述，基于python进行对改论文核心方法进行复刻。论文的目的是根据控制方程的形式，通过聚类方法和
稀疏PCA方法，发现各个点的控制项，也就是发现同一个计算域中不同区域的控制方程。

该论文的核心方法分为三个步骤：
1 对应与聚类
1.1 写出控制方程（PDE）。由于常见的PDE都可以看作n项不同的项相加等于0，论文将所有被加项看作特征值,由于控制方程对每一点上都成立，故每一点都对应了n个特征值，将特征值有序列视为n维空间中的
    坐标与物理空间中的该点对应。
1.2 论文使用GaussianMixtureModel方法，根据n维特征值的坐标位置对点进行聚类，特征相近的点便会划为同一聚类。

2 合并
  由于同一计算域中，不同控制方程的数量大概率小于聚类的个数，故需要对不同聚类进行合并。合并的根据是各聚类中活跃特征项，相同活跃项的聚类合并为同一类，该技术通常使用的是PCA方法，论文使用
  的是SparsePCA，一个聚类中有m个点，每个点有n个特征，可看作一个m*n的矩阵，使用SPCA（SparsePCA）方法，将其降维转化为1*n的向量，获得每个特征项的活跃程度。这个方法的优点是能非活跃项直接
  化为0。
